/**
 * config.js
 * 游戏配置文件，包含游戏的常量和配置信息
 */

/**
 * 游戏常量配置
 */
const CONFIG = {
    // 游戏基本设置
    BOARD_SIZE: 4,                       // 游戏棋盘大小 (4x4)
    WIN_TILE: 2048,                       // 获胜的方块数值
    MIN_SWIPE_DISTANCE: 30,               // 最小滑动距离（像素）
    
    // 本地存储键名
    STORAGE_KEYS: {
        BEST_SCORE: '2048_bestScore_v3',       // 最高分数存储键名
        BEST_MOVES: '2048_bestScoreMoves_v3',  // 达到最高分数的步数存储键名
        CURRENT_BOARD: '2048_currentBoard_v3', // 当前棋盘状态存储键名
        CURRENT_SCORE: '2048_currentScore_v3', // 当前分数存储键名
        MOVES_MADE: '2048_movesMade_v3'        // 当前步数存储键名
    },
    
    // 游戏介绍文本
    GAME_INTRO: {
        MAIN: `
1.  **游戏目标：**
       在一个 **4x4 的方格棋盘**上，通过移动和合并带有数字的方块，最终**组合出一个数值为 2048 的方块**。达到2048后，游戏将进入无尽模式，您可以继续挑战更高分！
2.  **核心玩法：**
       **移动：** 玩家每次操作可以选择 **向上、向下、向左、向右** 四个方向之一滑动。滑动时，棋盘上的所有方块会同时向该方向移动。
       **合并：** 当**两个相同数字的方块在移动过程中发生碰撞**时，它们会**合并成一个新的方块**，新方块的数字是它们之和（例如：两个 \`2\` 合并成 \`4\`；两个 \`8\` 合并成 \`16\`）。
       **生成新方块：** 每次有效移动（即至少有一个方块移动或合并了）后，系统会在棋盘的空位**随机生成一个新的方块**。这个新方块通常是 \`2\`（概率较高）或 \`4\`（概率较低）。
       **连锁反应：** 一次滑动操作可能引发多次合并（例如，一次滑动让两对 \`2\` 分别合并成 \`4\`）。
3.  **游戏规则/机制：**
       方块只能向空白位置或相同数字的方块移动（从而触发合并）。
       不同数字的方块不能合并。
       移动方向上的所有方块会尽可能移动到最远端（紧贴边界或被其他方块阻挡为止）。
       如果棋盘被填满，且**没有任何相邻的方块是相同数字**（即没有任何移动可以触发合并），则**游戏结束**。
4.  **策略要点：**
       **规划移动路径：** 需要思考如何移动方块，使得相同数字能够相遇合并，并为后续合并腾出空间或创造机会。
       **控制大数字的位置：** 通常策略是将最大的数字固定在一个角落（比如右下角），并围绕它构建递增的数字序列，避免大数字阻挡关键路径。
       **保持棋盘有序：** 尽量让数字按大小顺序排列（如从大到小或从小到大），减少混乱。
       **避免随机生成：** 尽量避免在关键位置出现随机生成的 \`2\` 或 \`4\` 打乱布局，但这部分有一定运气成分。
5.  **起源与流行：**
       由意大利开发者 **Gabriele Cirulli** 在 **2014年** 创建。
       最初是一个网页游戏，因其简单易上手、规则清晰但极具策略深度和挑战性而迅速风靡全球。
       现在有各种平台的版本（网页、手机App等）和无数变种（不同棋盘大小、不同合并规则等）。
**总结来说：**
2048 是一款极简风格的益智拼图游戏。玩家通过上下左右滑动操作，合并相同数字的方块（2+2=4, 4+4=8...），目标是合成 2048。它规则简单，但需要策略规划和空间布局能力，容易让人上瘾。游戏结束于棋盘被填满且无法再进行任何合并时。
玩过几次后，你会发现它"易学难精"的魅力所在！
`,
        
        SECRET_MESSAGE: "你知道吗？这是Ben送给skr的六一礼物，希望她天天开心！",
        
        PROJECT_INFO: `项目名称：2048<br>
项目全称：Easy2048<br>
项目版本：test9<br>
项目作者：<a href="https://github.com/ThatMrBen"> @本先森Ben（ThatMrBen）</a><br>
项目确立时间：北京时间 公元2024年5月25日<br>
版本更新日期：北京时间 公元2024年6月1日<br>
本次更新说明：<br>
1. 新增无尽模式：达到2048后游戏可继续进行，挑战更高分数！<br>
2. 界面细节优化：调整了部分元素间距。`
    },
    
    // 按钮图标
    ICONS: {
        INFO: '📄',
        SAVE: '💾',
        RESTART: '🔄'
    }
};